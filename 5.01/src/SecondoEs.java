import java.util.Scanner;

public class SecondoEs {
    Scanner scanner = new Scanner(System.in);
    String x = scanner.nextLine();
    String y = scanner.nextLine();

    public static void unit(String x, String y) {
        System.out.println(x + y);
    }

    ;
}
